package com.library.domain;

/**
 * Represents the type of media that can be borrowed in the library system.
 * <p>
 * This enum is used to distinguish between different item types
 * such as books and CDs, allowing fine calculation and loan handling
 * to vary based on media type.
 * </p>
 *
 * @author Maram
 * @version 1.0
 */
public enum MediaType {
    BOOK,
    CD
}
